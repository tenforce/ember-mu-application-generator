<div class="edit">
  <h2 class="title">{{t 'Edit <%= entityName %>'}}</h2>
  <div class="actions">
    <button type="button" class="btn save" title="Save new <%= entityName %>" {{action 'save' model}}>Save</button>
    <button type="button" class="btn cancel" title="Cancel" {{action 'cancel' model}}>Cancel</button>
    <button type="button" class="btn delete" title="Delete this item" {{action 'delete' model}}>Delete</button>
  </div>
  <table>
    <thead>
      <tr>
        <th>Attribute</th>
        <th>Data</th>
      </tr>
    </thead>
    <tbody>
      <%=
        // this currently only understands regular inputs
        attributes.map( function(attribute) {
          return "<tr><td>" + attribute.name + "</td><td align='middle'>{{input value=model." + attribute.itemVar +" placeholder=attribute.name}}</td></tr>";
        }).join("\n  ")
      %>
    </tbody>
  </table>
</div>





{{!-- TODO: relationships and lang-string-set!!! --}}
    {{!-- <%=
    attributes.map( function(attribute) {
      if(attribute.kind == "language-string-set") {
        return "<div class=\"row\"><div class=\"col s4\">" + attribute.name + "</div>"
    + "<div class=\"col s8\">"
    + "{{#each model." + attribute.itemVar + " as |label index|}}"
    + "<div class=\"row\">"
    + "<div class=\"col s8 field--location\">{{input type=\"text\" value=label.content}}</div>"
    + "<div class=\"col s4 field--language\">{{input type=\"text\" value=label.language}}</div>"
    + "</div>"
    + "{{/each}}</div></div>";
    }
    }).join("\n  ")

    %> --}}
