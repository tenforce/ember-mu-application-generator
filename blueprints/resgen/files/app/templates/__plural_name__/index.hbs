<div class="content">
  <h2 class="title">{{t '<%= entitiesName %>'}}</h2>
  <table>
    <thead>
      {{display-<%= entityName %>-attributes-header model=model}}
    </thead>
    <tbody>
      {{#each model as |item|}}
        {{display-<%= entityName %>-attributes-row model=item}}
      {{/each}}
    </tbody>
  </table>
</div>
