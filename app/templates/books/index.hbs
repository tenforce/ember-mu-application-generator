<div class="content">
  <h2 class="title">{{t 'books'}}</h2>
  {{#unless }}
    <div class="actions">
      {{#link-to "books.new" class="btn new" title="Create new item" tagName='button' }}Create{{/link-to}}
    </div>
  {{/unless}}
  {{#if model}}
    <table>
      <thead>
        <tr>
          <th class="properties__label">id</th>
            <th class="properties__label">{{t 'title'}}</th>
  <th class="properties__label">{{t 'isbn'}}</th>
            
              <th class="properties__label">{{t 'authors'}}</th>
        </tr>
      </thead>
      <tbody>
        {{#each model as |item|}}
          {{#link-to "books.show" item tagName='tr' }}
            {{handle-attribute model=item attribute='id' tagName='td' classes='properties__data'}}
            {{handle-attribute model=item attribute='title' tagName='td' classes='properties__data'}}
            {{handle-attribute model=item attribute='isbn' tagName='td' classes='properties__data'}}

            {{display-model-relationship model=item.authors tagName='td' kind='has-many' itemRoute='authors.show' relationRoute='authors.index' relationFilterKey='book' relationFilterValue=model.id configuredLabel='stringRep'}}
          {{/link-to}}
        {{/each}}
      </tbody>
    </table>
  {{else}}
    <div class="empty">
      No {{t 'books'}} in the database.
      {{#unless }} Click on the <i class="fa fa-plus" aria-hidden="true"></i> button to create a new {{t 'book'}}.
      {{/unless}}
    </div>
  {{/if}}
</div>
